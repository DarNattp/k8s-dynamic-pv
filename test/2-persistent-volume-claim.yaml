---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  namespace: staging
  name: test-claim
spec:
  storageClassName: nfs
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 10Mi
